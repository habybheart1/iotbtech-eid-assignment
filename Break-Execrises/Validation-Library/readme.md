# Question1 : Build a Validation Library
Create validators for: email, phone number (multiple formats), strong password, URL, and date (YYYY-MM-DD). Each must return:
{ valid: boolean, error: string | null }

- Answer: 
1. The return validator is an object
2. each propery have the conditions they want to check for